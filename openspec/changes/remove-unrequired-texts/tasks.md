# Tasks: Remove Unrequired UI Texts

- [ ] Remove Step 1 texts from `src/components/organisms/BookingCalendar.tsx`.
- [ ] Remove Step 2 texts from `src/components/organisms/BookingForm.tsx`.
- [ ] Verify the layout looks clean and correctly spaced after removals.
- [ ] (Optional) Run any linting or build tools to ensure no regressions.
