# Tasks: Spanish Translation

- [x] **Component Translation**
    - [x] Import `es` locale from `date-fns/locale` in `src/components/BookingCalendar.tsx`.
    - [x] Configure `Calendar` component with `locale={es}`.
    - [x] Translate all string literals in the component.

- [x] **Specification Update**
    - [x] Update `openspec/project.md` to establish Spanish as the primary language.
    - [x] Add a new requirement to `openspec/specs/language/spec.md` regarding language standards.

- [x] **Validation**
    - [x] Verify that the calendar widget (months/days) is in Spanish.
    - [x] Verify that all status labels and descriptions are in Spanish.
    - [x] Run `openspec validate translate-calendar-spanish`.
